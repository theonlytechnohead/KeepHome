<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="KeepHome.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Make the app responsible for drawing status bar and navigation bar backgrounds. -->
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="lollipop">true</item>
        <!-- Change the status bar background to whatever you want. -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/primary</item>
        <item name="android:navigationBarColor">@color/dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="PreferenceTheme.Dark" parent="KeepHome.Dark">
        <!-- Change the status bar background to whatever you want. -->
        <item name="android:statusBarColor" tools:targetApi="lollipop">?android:colorBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
